static long loop1(int max) {
	for (int i = 0; i < max; i++) {
		if (Pf(max, cnt)) {
			goto label;	//クラスファイル書換えにより挿入
		}
		cnt = (int) Math.sqrt(cnt);
		while (cnt < 5) {
			val1 += max;
label:
			val2 *= cnt;
			cnt++;
		}
	}
	return val1 + val2;
}