private static void swap(int i, int j) {
	int tmp = array[i];
	array[i] = array[j];
	array[j] = tmp;
}

@Obfuscate
public void test() {
	for (int index = 1; index < n - 1; index++) {
		swap(index, n - 1 - index);
		swap(index + 1, n - 1 - index);
		swap(index - 1, index + 1);
		swap(index, n - 1 - index);
		swap(index, index + 1);
		swap(index, index - 1);
	}
	return;
}
